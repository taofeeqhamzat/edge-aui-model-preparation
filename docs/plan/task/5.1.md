#### Task 5.1: Dynamic ONNX Export & INT8 Post-Training Quantization (`/src/export.py`)

- **Description:** Export PyTorch model to ONNX (`dynamic_axes={"input": {0: "batch_size", 1: "seq_len"}}`) and apply INT8 dynamic Post-Training Quantization (`quantize_dynamic` to `QUInt8`).
- **Acceptance Criteria:**
  - Exports both ONNX FP32 and INT8 quantized models to `models/`.
  - Input tensor shape configured for 18-dimensional features.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); import export; print('Export module verified')"`
- **Dependencies:** Task 4.1
- **Files Touched:**
  - `src/export.py`
- **Estimated Scope:** S (1 file)
