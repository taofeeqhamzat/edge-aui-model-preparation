#### Task 2.1: Canonical Event Schema & Viewport Normalization (`/src/preprocessing.py`)

- **Description:** Implement Layer A $\rightarrow$ Layer B transformation. Parse raw AdSERP CSV streams and XML metadata, extract canonical events (`timestamp_ms`, `event_type`, `x_norm`, `y_norm`, `xpath`), and normalize coordinates against observed source viewport dimensions (`<window>WxH</window>`) to canvas coordinates in $[0, 1]$.
- **Acceptance Criteria:**
  - Normalizes coordinate offsets relative to observed source viewport before optional $1920 \times 1080$ reference mapping.
  - Retains semantic event types (`mousemove`, `mouseover`, `click`, `load`, `scroll`).
  - Unit tests verify $[0, 1]$ coordinate bounds.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); import preprocessing; evs = preprocessing.parse_adserp_session('p004-b1-t1.csv'); print('Parsed events:', len(evs))"`
- **Dependencies:** Task 1.3
- **Files Touched:**
  - `src/preprocessing.py`
- **Estimated Scope:** M (1 file)
