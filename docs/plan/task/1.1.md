#### Task 1.1: Update DVC Remote Data Repository & Pipeline Stages

- **Description:** Update `.dvc/config` and `dvc.yaml` to formalize remote data tracking (`hfremote` pointing to Hugging Face S3-compatible gateway), support `dvc pull` / `dvc push` for `.data/`, and define declarative multi-stage pipeline steps (`data_sync`, `preprocess`, `train_foundation`, `fine_tune_ablation`, `export`).
- **Acceptance Criteria:**
  - `dvc.yaml` defines modular stages matching the new pipeline with explicit dependencies and outputs.
  - `.dvc/config` correctly specifies `hfremote` (`s3://edge-aui-framework-data/dvc-store`) with endpoint `https://s3.hf.co/T40`.
  - Clean error handling when DVC credentials are not locally set, gracefully falling back to local cached data or Hugging Face Hub git clone / snapshot.
- **Verification:**
  - Run: `dvc status` (or `dvc --version`).
  - Verify syntax and stage dependencies in `dvc.yaml`.
- **Dependencies:** None.
- **Files Touched:**
  - `dvc.yaml`
  - `.dvc/config`
  - `setup_hf_s3_dvc.sh`
- **Estimated Scope:** S (2 files)
