#### Task 1.2: Central Configuration System (`/src/config.yaml` & `/src/config.py`)

- **Description:** Build the central configuration system supporting `mode: minimal` (MVP: `AdSERP` $\rightarrow$ `Target UI Testbed`) and `mode: extended` (`CaptchaSolve30k` $\rightarrow$ `VideoCUA` $\rightarrow$ `AdSERP` $\rightarrow$ `Target UI Testbed`). Include dataclass validation and CLI override parsing.
- **Acceptance Criteria:**
  - `src/config.yaml` defines default parameters for windowing (500ms), stride (250ms), horizon (500–1500ms), feature dimensions (18), GRU hidden dim (64), batch size (64), and training modes.
  - `src/config.py` loads YAML/JSON and returns strongly-typed configuration objects with CLI override capabilities.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); from config import load_config; cfg = load_config('minimal'); print('Config loaded:', cfg.mode, 'Input dim:', cfg.input_dim)"`
- **Dependencies:** None.
- **Files Touched:**
  - `src/config.yaml`
  - `src/config.py`
  - `requirements.txt` (add `pyyaml`, `pyarrow`)
- **Estimated Scope:** S (2 files)
