#### Task 1.3: Data Ingestion & Inode-Safe Consolidation (`/src/data.py`)

- **Description:** Implement the data access layer that resolves datasets (`AdSERP` from local `../data-sources/adserp-2025` or remote DVC/Hugging Face), partitions data into `.data/raw`, `.data/interim`, `.data/processed`, and consolidates high-volume session logs into compressed Parquet tables to eliminate inode exhaustion.
- **Acceptance Criteria:**
  - Resolves AdSERP CSV logs (`mouse-movement-data/*.csv`) and XML metadata (`trial-metadata/*.xml`).
  - Consolidates parsed session streams into `.data/interim/adserp_consolidated.parquet` without creating thousands of tiny files.
  - Seamlessly integrates with DVC remote pull commands.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); import data; path = data.ensure_adserp_dataset(); print('AdSERP data path:', path)"`
- **Dependencies:** Task 1.1, Task 1.2
- **Files Touched:**
  - `src/data.py`
- **Estimated Scope:** M (1 file + tests)
