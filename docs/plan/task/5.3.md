#### Task 5.3: Top-Level Pipeline Orchestrator (`train_pipeline.py`) & Notebook Sync

- **Description:** Build `train_pipeline.py` CLI supporting `--mode [minimal|extended]`, `--config src/config.yaml`, `--epochs`, `--device`. Update `generate_training_nb.py` and `notebooks/Training.ipynb` to execute the new pipeline and link directly to branch `explore/pipeline/revision/1`.
- **Acceptance Criteria:**
  - Single command executes end-to-end: data validation $\rightarrow$ preprocessing $\rightarrow$ target labeling $\rightarrow$ training $\rightarrow$ ablation $\rightarrow$ export.
  - Training notebook reflects the updated pipeline with Colab badge pointing to `explore/pipeline/revision/1`.
- **Verification:**
  - Run: `python3 train_pipeline.py --mode minimal --epochs 1`
  - Run: `python3 generate_training_nb.py`
- **Dependencies:** Task 1.1–5.2
- **Files Touched:**
  - `train_pipeline.py`
  - `generate_training_nb.py`
  - `notebooks/Training.ipynb`
- **Estimated Scope:** M (3 files)
