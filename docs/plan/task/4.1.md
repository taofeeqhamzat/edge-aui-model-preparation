#### Task 4.1: Decoupled GRU Latent Encoder Architecture (`/src/training.py`)

- **Description:** Build `EdgeAUIGRU` with `input_dim=18`, `hidden_dim=64`, `num_layers=2`. The recurrent backbone produces latent behavioral embedding $h_T \in \mathbb{R}^{64}$. Attach modular heads:
  1. `FoundationOutcomeHead`: maps $h_T \rightarrow 6$ outcome classes (`CLICK`, `IDLE`, etc.).
  2. `TargetInterventionHead`: maps $h_T \rightarrow 5$ target interface actions (`simplify_options`, `highlight_primary_action`, `offer_assistance`, `expand_tooltip`, `no_op`).
- **Acceptance Criteria:**
  - Latent representation $h_T$ is completely interface-agnostic (resolves target OOV problem).
  - Clean separation between backbone parameters $\theta_{\text{base}}$ and head parameters $\theta_{\text{head}}$.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); import training, torch; m = training.EdgeAUIGRU(18, 64, 2, 6); x = torch.randn(2, 8, 18); out, h = m(x, return_latent=True); assert h.shape == (2, 64); print('Forward pass & latent extraction OK')"`
- **Dependencies:** Task 3.1
- **Files Touched:**
  - `src/training.py`
- **Estimated Scope:** M (1 file)
