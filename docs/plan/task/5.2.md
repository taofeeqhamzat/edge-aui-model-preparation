#### Task 5.2: Edge Constraint Profiler without Hardcoded Assertions (`/src/export.py`)

- **Description:** Profile the quantized INT8 model for edge constraints:
  - `artifact_size_bytes` and memory footprint (verifying $< 20\text{ MB}$, target $< 200\text{ KB}$).
  - `parameter_count`.
  - p50 and p95 inference latency across 100 iterations on CPU execution provider.
  - Replace rigid assertions with structured warning/informational logging to prevent CI/local build failures under variable CPU load.
- **Acceptance Criteria:**
  - Reports size, parameter count, and p50/p95 latency in a structured dict/table.
  - Logs advisory messages rather than crashing on minor host latency spikes.
- **Verification:**
  - Run: `python3 src/export.py --model-path models/foundational_gru.pth --output-dir models`
- **Dependencies:** Task 5.1
- **Files Touched:**
  - `src/export.py`
- **Estimated Scope:** S (1 file)
