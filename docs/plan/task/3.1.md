#### Task 3.1: Derived Future-Outcome Labeler (`/src/target_generation.py`)

- **Description:** Implement self-supervised outcome generation. For every 500ms MicroTensor sequence, inspect the subsequent lookahead horizon $\Delta t \in [500\text{ms}, 1500\text{ms}]$ to extract verifiable downstream UI macro-interactions (`IDLE_ABANDON`, `CLICK`, `FORM_SUBMIT`, `BACKTRACK`, `RAPID_SCROLL`, `HOVER_DWELL`). Zero subjective affective labels.
- **Acceptance Criteria:**
  - Identifies downstream verifiable UI macro-actions strictly within $\Delta t \in [500\text{ms}, 1500\text{ms}]$.
  - Pairs sequence tensor $(T, 18)$ with discrete outcome label $y \in \{0, 1, 2, 3, 4, 5\}$.
  - Provides PyTorch `Dataset` integration for training.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); import target_generation; ds = target_generation.create_sample_outcome_dataset(); print('Outcome dataset size:', len(ds))"`
- **Dependencies:** Task 2.2
- **Files Touched:**
  - `src/target_generation.py`
- **Estimated Scope:** M (1 file)
