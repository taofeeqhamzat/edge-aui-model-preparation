#### Task 4.2: Progressive Unfreezing Ablation Suite (`/src/training.py`)

- **Description:** Implement the progressive fine-tuning ablation experiments:
  1. **Strict Freezing:** Freeze recurrent backbone ($\frac{\partial \mathcal{L}}{\partial \theta_{\text{base}}} = 0$); train exclusively target linear head.
  2. **Partial Fine-Tuning:** Unfreeze terminal GRU layer + target head; differential LR (GRU LR: `1e-4`, Head LR: `1e-3`).
  3. **Full Fine-Tuning:** End-to-end updates across all layers.
     Evaluate and log Hit Rate@1, Hit Rate@3, and MRR across all strategies without failing assertions.
- **Acceptance Criteria:**
  - Implements all 3 fine-tuning strategies cleanly.
  - Computes $\text{HR}@1$, $\text{HR}@3$, and $\text{MRR}$ on target testbed splits.
  - Outputs ablation comparison summary table.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); import training; results = training.run_ablation_suite(dummy=True); print('Ablation strategies tested:', list(results.keys()))"`
- **Dependencies:** Task 4.1
- **Files Touched:**
  - `src/training.py`
- **Estimated Scope:** M (1 file)
