#### Task 2.2: MicroTensor Extraction with Modality Masking ($2D=18$)

- **Description:** Implement Layer B $\rightarrow$ Layer C extraction. Segment canonical event streams into 500ms windows with 250ms stride. Calculate 7 core kinematic features + 2 optional contextual features. Generate binary Modality Mask Vector $M \in \{0, 1\}^9$ and output concatenated vector $\widetilde{X}_t = [X_t \odot M, \; M] \in \mathbb{R}^{18}$.
- **Acceptance Criteria:**
  - Core features extracted: Euclidean mean velocity, max velocity, acceleration, hesitation count ($>45^\circ$), total length, dwell time, trajectory entropy.
  - Modality mask vector correctly signals presence ($1$) or absence ($0$) of contextual features.
  - Final tensor output has shape `(seq_len, 18)` and values bounded in $[0, 1]$.
- **Verification:**
  - Run: `python3 -c "import sys; sys.path.insert(0, 'src'); import preprocessing; t = preprocessing.extract_mock_microtensor(); assert t.shape[-1] == 18; print('MicroTensor shape OK:', t.shape)"`
- **Dependencies:** Task 2.1
- **Files Touched:**
  - `src/preprocessing.py`
- **Estimated Scope:** M (1 file)
